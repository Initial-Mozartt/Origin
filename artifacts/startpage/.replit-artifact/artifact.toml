kind = "web"
previewPath = "/"
title = "Origin"
version = "1.0.0"
id = "artifacts/startpage"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25606

[services.development]
run = "pnpm --filter @workspace/startpage run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/startpage", "run", "build" ]
publicDir = "artifacts/startpage/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25606"
BASE_PATH = "/"
